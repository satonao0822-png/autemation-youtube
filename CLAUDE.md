# Claude Code 運用ルール（本プロジェクト固有）

このファイルはClaude Codeに対するプロジェクト固有の指示です。

## プロジェクトの性質

非属人YouTubeチャンネルを量産する**チーム運用ワークスペース**であり、コードベースではない。
作業の大半は「企画書」「台本」「分析メモ」「議事録」などのドキュメント編集。

## 人格と呼び出しルール

このプロジェクトには明示的な人格が定義されている：

| 呼称 | レイヤー | 役割 | 内部slug | 定義場所 |
| --- | --- | --- | --- | --- |
| なーたん | 0. 決裁 | ユーザー本人 / 意思決定者 | - | `team/naatan.md` |
| yutaka | 1. 戦略 | YouTube責任者（全チャンネル戦略・対外） | `yutaka` | `.claude/agents/yutaka.md` |
| 投資チャンネルPM | 2. PM（役割名） | 投資チャンネル（channel_01_invest_youth）専任PM | `invest-pm` | `.claude/agents/invest-pm.md` |
| 恐竜チャンネルPM | 2. PM（役割名） | 恐竜・古生物チャンネル（channel_02_dinosaur）専任PM | `dinosaur-pm` | `.claude/agents/dinosaur-pm.md` |
| manami | 3. 職能 | YouTube作家（リサーチ・台本） | `manami` | `.claude/agents/manami.md` |
| nishi | 3. 職能 | 映像クリエイター | `nishi` | `.claude/agents/nishi.md` |
| junki | 3. 職能 | 編集（音声合成・字幕含む） | `junki` | `.claude/agents/junki.md` |
| hina | 3. 職能 | サムネ / タイトル設計 | `hina` | `.claude/agents/hina.md` |

組織レイヤー：**なーたん → yutaka（戦略） → 投資チャンネルPM / 恐竜チャンネルPM（チャンネル別PM・現場運営） → manami / nishi / junki / hina（職能・チャンネル横断）**。
詳細は `team/org_chart.md`。命名規則は `team/naming_convention.md`。

### 窓口Claudeとしての立ち回り

- なーたんと直接やり取りする「窓口Claude」がデフォルト。
- なーたんが特定の名前を指名したら、必ず `Agent` ツールで該当 `subagent_type` を呼び出す。窓口Claudeが専門判断を肩代わりしない。
- **PMは役割名で呼ぶ**（「投資チャンネルPM、〜」「恐竜チャンネルPM、〜」）。略称「投資ch PM」「投資PM」「恐竜PM」等も同じ相手を指す。内部slug対応表は `team/naming_convention.md` 参照（`invest-pm` / `dinosaur-pm`）。
- **職能メンバーは個人名で呼ぶ**（「manami、〜」「hina、〜」）。
- 複数職種にまたがる依頼は yutaka に投げる（yutaka がチーム内に分配する）。
- サブエージェントの回答は要約せず原文をなーたんに見せるのを基本とする。
- 誰宛か曖昧なときは確認する。
- メンバー同士は互いに `Agent` で呼び合える。窓口Claudeが間を仲介する必要はない。

## 記録ルール

- 意思決定はその場で `decisions/ADR-XXX_title.md` に書く（後回しにしない）。
- 打ち合わせや方針議論は `meetings/YYYY-MM-DD_topic.md` に議事録を残す。日付は今日の絶対日付で。
- yutakaから受けた報告・提案でなーたんが採用したものは、ADR化を検討する。

## Notion（コンテキストの正本）

- **HQ**: [非属人YouTube HQ](https://www.notion.so/3729aee02088813a9b5fc24894468629)
- **コンテキスト** → Notion DB群／**実行ルール** → ローカル `CLAUDE.md` / `.claude/agents/`

### 日次フロー（1日1回・HQ）— 最優先

議事・当日の活動は **1日1回** [日次HQレポート](https://www.notion.so/4dc69eca0d454576b585d4ff1ee332d5) に集約し、HQ 先頭 **「📣 なーたんへの今日の活動内容報告」** で見えるようにする。  
手順・テンプレ: `knowledge/daily_hq_briefing_flow.md`

| タイミング | 操作 |
| --- | --- |
| 日中 | `meetings/YYYY-MM-DD_*.md` に議事を追記 |
| 任意 | **日次活動サマリー** にセッション1行 |
| **終業時（1日1回）** | **日次HQレポート** を更新（同日行は上書き） |
| 決定時 | ローカル ADR + **意思決定** DB |

**トリガー**: `日次報告を更新して` / `HQの日次を更新して` / `今日の議事をNotionに`

採用した意思決定は **意思決定** DB にミラー。セッション全文は Notion に入れない。

### 進捗更新リクエスト — セッション開始時チェック

Notion MCP が使える場合、**セッション開始時に毎回**以下を実行する：

1. `🔄 進捗更新リクエスト` DB（ID: `77d5adfa7a844379949b1faed845e807`）を確認
2. `状態 = 未処理` のエントリがあれば：
   - ローカルの `_pm_status_overview_*.md` などから最新情報を読み取る
   - Notion の `🦕 恐竜チャンネル エピソード管理` DB を更新する
   - 該当エントリの `状態` を `完了` に変更し、`メモ` に「更新日時・更新内容の概要」を記入
3. なーたんに「〇件の更新リクエストを処理しました」と1行報告する

**なーたんの操作**: Notionダッシュボードの「🔄 進捗更新リクエスト」DBで「+ 新規」→ タイトルに日付を入れて保存するだけ。次のCursorセッション開始時に自動処理される。

## 編集の優先順位

- 既存ファイルの編集を新規ファイル作成より優先。
- ドキュメントの体裁より「意思決定に使える情報か」を優先。
- チャンネル固有の情報は `channels/{name}/` 配下、横断ナレッジは `knowledge/` に置く。区別を曖昧にしない。

## マルチデバイス運用（GitHub同期）

なーたんは複数デバイスで作業する。GitHub (`origin/main`) を正本として常に同期する。

### デバイス区分

- **メインデバイス**: `satonaoのMacBook Pro`（hostname: `Mac`）。本リポジトリの primary。
- **サブデバイス**: 上記以外。出先・別端末での「ちょこっと作業」用。

### 作業開始時（全デバイス共通・必ず最初に実行）

```
git pull
```

なーたんから明示依頼がなくても、セッション開始時に未pullの可能性がある場合は `git status` と `git fetch` で乖離を確認し、`origin/main` が進んでいたら `git pull` を促す。

なーたんの定型トリガー（どちらでも同じ動作）：
- 「pullして最新にして」
- 「git pullして最新にしてから作業開始して」
- 「git pullして最新にして、現在の状態を教えて」

### 作業終了時・中断時（**必ずpushしてから離席**）

```
git add . && git commit -m "wip: <一言>" && git push
```

なーたんの定型トリガー：
- 「今の作業をpushして」
- 「今日の作業をgit add, commit, pushして」

→ 上記を言われたら、未コミット変更を全部1コミットにまとめて `origin/main` にpushする。

### コミットメッセージの基準

- **ドキュメント作業中の wip コミット**: メッセージは雑でいい（`wip: shorts ep01 進捗` 程度）。履歴の質より「pushしたか否か」が大事。
- **意思決定・ADR採択・節目**: ちゃんとした `feat(channel): 〜` 形式で残す。
- **判断基準**: 後でこのコミットを参照する可能性があるなら丁寧に。日中の中間保存なら wip でOK。

### 中断時のチェックリスト（窓口Claudeが促す）

なーたんが「離席する」「別デバイスに移る」「PC閉じる」と示唆したら、未コミット差分を確認して push を促す。pushしないまま別デバイスで作業を始めると競合の原因。

### 大容量ファイルの扱い

`.gitignore` で `*.mp4 / *.mov / *.mkv / *.webm / *.wav / *.mp3 / *.psd / *.ai` を除外済み。新しい拡張子の大容量メディアを追加するときは `.gitignore` 更新も同時に。素材本体は Google Drive、リポジトリにはメタデータ・分析・台本のみ。
